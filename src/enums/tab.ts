export enum HistogramSettingsTabs {
    STYLING,
    CONFIG
}

export enum SpatialProfilerSettingsTabs {
    STYLING,
    SMOOTHING,
    COMPUTATION
}

export enum SpectralProfilerSettingsTabs {
    CONVERSION,
    STYLING,
    SMOOTHING,
    MOMENTS,
    FITTING
}

export enum StokesAnalysisSettingsTabs {
    CONVERSION,
    LINE_PLOT_STYLING,
    SCATTER_PLOT_STYLING,
    SMOOTHING
}

export enum CatalogSettingsTabs {
    GLOBAL,
    STYLING,
    COLOR,
    SIZE,
    ORIENTATION,
    SIZE_MAJOR,
    SIZE_MINOR,
    ANGULAR_SIZE
}

export enum LayerListSettingsTabs {
    MATCHING,
    REST_FREQ
}

export enum ImageViewSettingsPanelTabs {
    PAN_AND_ZOOM = "Pan and Zoom",
    GLOBAL = "Global",
    TITLE = "Title",
    TICKS = "Ticks",
    GRIDS = "Grids",
    BORDER = "Border",
    AXES = "Axes",
    NUMBERS = "Numbers",
    LABELS = "Labels",
    BEAM = "Beam",
    COLORBAR = "Colorbar",
    CONVERSION = "Conversion"
}

export enum PreferenceDialogTabs {
    GLOBAL,
    RENDER_CONFIG,
    CONTOUR_CONFIG,
    VECTOR_OVERLAY_CONFIG,
    WCS_OVERLAY_CONFIG,
    LAYOUT,
    REGION,
    ANNOTATION,
    PERFORMANCE,
    LOG_EVENT,
    CATALOG,
    STATISTICS,
    TELEMETRY,
    COMPATIBILITY
}

export enum VectorOverlayDialogTabs {
    Configuration,
    Styling
}

export enum OnlineDataQueryDialogTabs {
    Catalog,
    Hips
}

export enum ContourDialogTabs {
    Levels,
    Configuration,
    Styling
}

export enum RegionDialogTabs {
    Configuration,
    Styling
}

export enum FittingResultTabs {
    RESULT,
    LOG
}
