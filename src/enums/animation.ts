export enum AnimationMode {
    NONE = -1,
    CHANNEL = 0,
    STOKES = 1,
    FRAME = 2,
    TIME_SERIES = 3
}

export enum PlayMode {
    FORWARD = 0,
    BACKWARD = 1,
    BOUNCING = 2,
    BLINK = 3
}

export enum NumericInputType {
    FrameRate = "Frame rate",
    Step = "Step"
}
