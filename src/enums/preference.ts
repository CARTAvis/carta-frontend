export enum CursorInfoVisibility {
    Always = "always",
    Never = "never",
    ActiveImage = "activeImage",
    HideTiled = "hideTiled"
}

export enum FileFilterMode {
    Content = "content",
    Extension = "extension",
    All = "all"
}
