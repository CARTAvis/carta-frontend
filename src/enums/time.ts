export enum TimeLabelFormat {
    AUTO = "auto",
    ISO = "iso",
    MJD = "mjd",
    JD = "jd",
    RELATIVE = "relative"
}

export enum TimeZoneMode {
    UTC = "utc",
    LOCAL = "local",
    IANA = "iana"
}

export enum TimeScale {
    UTC = "UTC",
    TAI = "TAI",
    TT = "TT",
    TCG = "TCG"
}

export enum IsoTimePrecision {
    AUTO = "auto",
    YEAR = "year",
    MONTH = "month",
    DAY = "day",
    HOUR = "hour",
    MINUTE = "minute",
    SECOND = "second",
    MILLISECOND = "millisecond",
    MICROSECOND = "microsecond"
}

export enum RelativeTimeReference {
    FIRST = "first",
    IMAGE = "image",
    CUSTOM = "custom"
}

export enum RelativeTimeUnit {
    AUTO = "auto",
    SECOND = "second",
    MINUTE = "minute",
    HOUR = "hour",
    DAY = "day",
    YEAR = "year"
}
