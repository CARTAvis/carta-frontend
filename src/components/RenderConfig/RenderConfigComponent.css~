/*
 * Copyright 2017 Palantir Technologies, Inc. All rights reserved.
 */
.equation-div {
  height: 20px;
  position: relative;
  background-size: auto 18px;
  background-repeat: no-repeat; }

.colormap-select-popover .bp3-menu {
  max-height: 180px;
  overflow: auto;
  padding: 0; }

.colormap-block {
  width: 75px;
  height: 15px;
  image-rendering: pixelated;
  -moz-image-rendering: crisp-edges;
  border-radius: 3px;
  border: 1px solid #a7b6c2; }
  .colormap-block.bp3-dark {
    border: 1px solid #738694; }

.render-config-container {
  display: flex;
  width: 100%;
  height: 100%; }
  .render-config-container .colormap-config {
    flex: 1 1 230px;
    min-width: 230px;
    padding-right: 10px;
    padding-top: 10px;
    overflow-y: auto;
    flex-wrap: nowrap;
    display: flex;
    flex-direction: column; }
    .render-config-container .colormap-config div {
      margin-left: auto; }
    .render-config-container .colormap-config .percentile-select {
      margin-right: 0; }
    .render-config-container .colormap-config .bp3-form-group {
      float: right;
      margin-bottom: 5px; }
      .render-config-container .colormap-config .bp3-form-group .bp3-button {
        width: 130px; }
      .render-config-container .colormap-config .bp3-form-group .bp3-label {
        margin-right: 5px; }
    .render-config-container .colormap-config .bp3-numeric-input .bp3-input-group input {
      width: 90px; }
    .render-config-container .colormap-config .bp3-input-group input {
      width: 120px; }
    .render-config-container .colormap-config select {
      width: 90px;
      direction: rtl; }
    .render-config-container .colormap-config .cursor-display pre {
      font-size: x-small; }
  .render-config-container .histogram-container {
    flex: 3 3 auto;
    min-width: 0;
    padding-bottom: 5px; }
    .render-config-container .histogram-container .histogram-plot {
      height: calc(100% - 35px);
      position: relative; }

.percentile-buttons {
  margin: 5px auto auto 5px;
  height: 30px; }

.percentile-select {
  margin: 5px auto auto 5px; }
  .percentile-select .bp3-form-group {
    margin: 0; }
  .percentile-select .bp3-button {
    margin-left: 5px; }
